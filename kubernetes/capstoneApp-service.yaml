---
apiVersion: v1
kind: Service
metadata:
  name: capstoneApp-service
  labels:
    name: capstoneApp-service
    app: capstoneApp-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: capstoneApp-pod
    app: capstoneApp-app
