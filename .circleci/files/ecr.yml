Description:
  Elastic Container Registry for CapstoneRepo.

Resources:
  ECR:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: "CapstoneRepo"
      ImageScanningConfiguration:
          ScanOnPush: true

Outputs:
  RepositoryUri:
    Value: !GetAtt ECR.RepositoryUri